<h4>{{data.prettyName}}</h4>

{{#if data.description}}
  {{data.description}}
{{else}}
  <i>No description available</i>
{{/if}}
<br>

<button {{action 'updateModal'}}>Edit</button>
<button {{action 'deleteAttribute'}}>Remove</button>
